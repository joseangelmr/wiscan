<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ldscsoft.wiscan" >

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/wiscan_launcher_icon"
        android:label="@string/app_name"
        android:theme="@style/WiScanActionBarTheme" >


        <meta-data android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.NetworkPlotActivity"
            android:label="@string/plot" >
        </activity>
        <activity
            android:name=".activities.ExperimentDataPlotActivity"
            android:label="@string/plot" >
        </activity>
        <activity
            android:name=".activities.AcercaDeActivity"
            android:label="@string/action_about"
            android:screenOrientation="portrait">
        </activity>

        <activity android:name=".preferencias.PreferenciasActivity"
            android:label="@string/action_settings">
        </activity>
    </application>
</manifest>